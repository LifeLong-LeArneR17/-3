import React from 'react';
import './Button.css';
export const Button = ({ totalPhotos, currentPage, onChangePage }) => {
  return (
    <button
      className="Button"
    >
      Load more
    </button>
  );
};